// Test JavaScript file for validation
const message = "Hello World"
let count = 0;

function greet(name) {
  return `Hello, ${name}!`;
}

// Test object with potential syntax issues
const config = {
  name: "test",
  value: 42
}

console.log(greet("PocketCode"));
